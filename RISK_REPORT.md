# France Daily Peak Price Risk Report

The live report will be generated by `risk_report.py` after the repository is uploaded and can reach Energy-Charts.

The default paper assumptions are defined transparently in `settings.json`; they are not market-provided limits.
